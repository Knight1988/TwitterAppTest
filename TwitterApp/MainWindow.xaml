<Window x:Class="TwitterApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        DataContext="{Binding Source={StaticResource Locator}, Path=MainViewModel}"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Total number of tweets received:" />
            <TextBlock Text="{Binding TweetCount, Mode=TwoWay}" />
            <TextBlock Text="Average tweets per minute:" />
            <TextBlock Text="{Binding AverageTweetPerMinute, Mode=TwoWay}" />
        </StackPanel>
    </Grid>
</Window>
